═══════════════════════════════════════════════════════════
   سحب التحديثات من GitHub إلى المشروع على VPS
═══════════════════════════════════════════════════════════

✅ بعد رفع الملفات إلى GitHub، يمكنك سحب التحديثات

═══════════════════════════════════════════════════════════
📥 الطريقة الأولى: سحب التحديثات يدوياً
═══════════════════════════════════════════════════════════

1️⃣  الاتصال بـ VPS:
   ssh username@your-vps-ip

2️⃣  الانتقال إلى مجلد المشروع:
   cd /var/www/alshabibi/storeoman

3️⃣  سحب التحديثات:
   git pull origin main

4️⃣  إعادة تشغيل Docker (إذا لزم):
   docker-compose restart
   # أو
   docker-compose up -d --build

═══════════════════════════════════════════════════════════
🔄 الطريقة الثانية: استخدام سكريبت تلقائي
═══════════════════════════════════════════════════════════

1️⃣  إنشاء ملف update.sh:
   nano /var/www/alshabibi/storeoman/update.sh

2️⃣  محتوى الملف:
   #!/bin/bash
   cd /var/www/alshabibi/storeoman
   git pull origin main
   docker-compose restart
   echo "✅ تم التحديث بنجاح!"

3️⃣  إعطاء صلاحيات التنفيذ:
   chmod +x update.sh

4️⃣  تشغيل السكريبت:
   ./update.sh

═══════════════════════════════════════════════════════════
📋 الأوامر السريعة
═══════════════════════════════════════════════════════════

# سحب التحديثات
cd /var/www/alshabibi/storeoman
git pull origin main

# إعادة تشغيل Docker
docker-compose restart

# أو سحب وإعادة تشغيل معاً
cd /var/www/alshabibi/storeoman && git pull origin main && docker-compose restart

═══════════════════════════════════════════════════════════
🔍 التحقق من التحديثات
═══════════════════════════════════════════════════════════

# عرض آخر التحديثات
git log --oneline -5

# عرض التعديلات قبل السحب
git fetch origin
git log HEAD..origin/main --oneline

# عرض الحالة
git status

═══════════════════════════════════════════════════════════
⚠️  حل المشاكل
═══════════════════════════════════════════════════════════

❌ المشكلة: "fatal: not a git repository"
   الحل:
   git init
   git remote add origin https://github.com/saraa47258-cmd/storeoman.git

❌ المشكلة: "error: Your local changes would be overwritten"
   الحل:
   # حفظ التعديلات المحلية
   git stash
   git pull origin main
   git stash pop

❌ المشكلة: "Permission denied"
   الحل:
   sudo chown -R $USER:$USER /var/www/alshabibi/storeoman

═══════════════════════════════════════════════════════════
🔄 سيناريوهات مختلفة
═══════════════════════════════════════════════════════════

1️⃣  سحب التحديثات فقط:
   git pull origin main

2️⃣  سحب وإعادة بناء Docker:
   git pull origin main
   docker-compose up -d --build

3️⃣  سحب مع حفظ التعديلات المحلية:
   git stash
   git pull origin main
   git stash pop

4️⃣  إعادة تعيين المشروع من GitHub (احذر!):
   git fetch origin
   git reset --hard origin/main

═══════════════════════════════════════════════════════════
✅ قائمة التحقق
═══════════════════════════════════════════════════════════

☑️  المشروع مربوط مع GitHub
☑️  Git مثبت على VPS
☑️  في المجلد الصحيح: /var/www/alshabibi/storeoman
☑️  تم سحب التحديثات
☑️  Docker يعمل بشكل صحيح

═══════════════════════════════════════════════════════════
