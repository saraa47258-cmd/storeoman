═══════════════════════════════════════════════════════════
   دليل شامل: ربط المشروع على VPS مع GitHub
   من البداية حتى سحب الملفات
═══════════════════════════════════════════════════════════

📌 هذا الدليل يشرح كل شيء من الصفر

═══════════════════════════════════════════════════════════
🔑 الخطوة 1: إنشاء Personal Access Token
═══════════════════════════════════════════════════════════

1. اذهب إلى: https://github.com/settings/tokens

2. انقر "Generate new token" > "Generate new token (classic)"

3. إعدادات Token:
   - Note: StoreOman VPS
   - Expiration: اختر المدة
   - Select scopes: اختر repo (كل الصلاحيات)

4. انقر "Generate token"

5. انسخ الـ Token واحفظه (لن يظهر مرة أخرى!)

═══════════════════════════════════════════════════════════
💻 الخطوة 2: الاتصال بـ VPS
═══════════════════════════════════════════════════════════

افتح Terminal أو SSH Client واتصل:

ssh username@your-vps-ip

مثال:
ssh root@192.168.1.100

═══════════════════════════════════════════════════════════
📦 الخطوة 3: تثبيت Git على VPS
═══════════════════════════════════════════════════════════

# التحقق من تثبيت Git
git --version

# إذا لم يكن مثبتاً:
sudo apt update
sudo apt install -y git

# إعداد Git (للمرة الأولى)
git config --global user.name "StoreOman VPS"
git config --global user.email "vps@storeoman.com"

═══════════════════════════════════════════════════════════
📂 الخطوة 4: الانتقال إلى مجلد المشروع
═══════════════════════════════════════════════════════════

cd /var/www/alshabibi/storeoman

# التحقق من أنك في المجلد الصحيح
pwd
# يجب أن يظهر: /var/www/alshabibi/storeoman

═══════════════════════════════════════════════════════════
🔧 الخطوة 5: تهيئة Git في المشروع
═══════════════════════════════════════════════════════════

# تهيئة Git
git init

# التحقق من التهيئة
ls -la | grep .git
# يجب أن ترى مجلد .git

═══════════════════════════════════════════════════════════
🔗 الخطوة 6: ربط المشروع مع GitHub
═══════════════════════════════════════════════════════════

# ربط مع GitHub
git remote add origin https://github.com/saraa47258-cmd/storeoman.git

# التحقق من الربط
git remote -v
# يجب أن ترى رابط GitHub

═══════════════════════════════════════════════════════════
💾 الخطوة 7: حفظ ملف .env (مهم جداً!)
═══════════════════════════════════════════════════════════

# حفظ ملف .env قبل سحب التحديثات
cp .env /tmp/storeoman.env.backup

# التحقق من الحفظ
ls -la /tmp/storeoman.env.backup

═══════════════════════════════════════════════════════════
📥 الخطوة 8: سحب الملفات من GitHub
═══════════════════════════════════════════════════════════

# سحب الملفات
git pull origin main

# عند طلب اسم المستخدم وكلمة المرور:
# Username: saraa47258-cmd
# Password: Personal Access Token (الذي أنشأته في الخطوة 1)

═══════════════════════════════════════════════════════════
🔄 الخطوة 9: حل مشكلة تعارض الملفات (إن وجدت)
═══════════════════════════════════════════════════════════

إذا ظهرت رسالة "untracked working tree files would be overwritten":

# استبدال جميع الملفات بالملفات من GitHub
git fetch origin
git reset --hard origin/main

# استعادة ملف .env
cp /tmp/storeoman.env.backup .env

═══════════════════════════════════════════════════════════
✅ الخطوة 10: التحقق من الملفات
═══════════════════════════════════════════════════════════

# عرض الملفات
ls -la

# يجب أن ترى جميع الملفات من GitHub:
# - index.html
# - styles.css
# - docker-compose.yml
# - config.php
# - وغيرها...

═══════════════════════════════════════════════════════════
🚀 الخطوة 11: إعادة تشغيل المشروع
═══════════════════════════════════════════════════════════

# إعادة تشغيل Docker
docker-compose restart

# أو إعادة بناء
docker-compose up -d --build

# التحقق من الحالة
docker-compose ps

═══════════════════════════════════════════════════════════
📋 الأوامر الكاملة (انسخ والصق)
═══════════════════════════════════════════════════════════

# 1. الاتصال بـ VPS
ssh username@your-vps-ip

# 2. تثبيت Git
sudo apt update
sudo apt install -y git

# 3. الانتقال إلى المجلد
cd /var/www/alshabibi/storeoman

# 4. تهيئة Git
git init
git config user.name "StoreOman VPS"
git config user.email "vps@storeoman.com"

# 5. ربط مع GitHub
git remote add origin https://github.com/saraa47258-cmd/storeoman.git

# 6. حفظ ملف .env
cp .env /tmp/storeoman.env.backup

# 7. سحب الملفات
git pull origin main

# 8. استعادة ملف .env
cp /tmp/storeoman.env.backup .env

# 9. إعادة تشغيل Docker
docker-compose restart

═══════════════════════════════════════════════════════════
🔄 التحديثات المستقبلية (سريع)
═══════════════════════════════════════════════════════════

cd /var/www/alshabibi/storeoman
cp .env /tmp/storeoman.env.backup
git pull origin main
cp /tmp/storeoman.env.backup .env
docker-compose restart

═══════════════════════════════════════════════════════════
🐛 حل المشاكل
═══════════════════════════════════════════════════════════

❌ "fatal: not a git repository"
   → cd /var/www/alshabibi/storeoman && git init

❌ "remote origin already exists"
   → git remote set-url origin https://github.com/saraa47258-cmd/storeoman.git

❌ "Authentication failed"
   → استخدم Personal Access Token وليس كلمة المرور

❌ "Permission denied"
   → sudo chown -R $USER:$USER /var/www/alshabibi/storeoman

═══════════════════════════════════════════════════════════
✅ قائمة التحقق
═══════════════════════════════════════════════════════════

☑️  Personal Access Token تم إنشاؤه
☑️  متصل بـ VPS
☑️  Git مثبت
☑️  في المجلد الصحيح
☑️  Git مهيأ
☑️  مربوط مع GitHub
☑️  ملف .env محفوظ
☑️  تم سحب الملفات
☑️  ملف .env مستعاد
☑️  Docker يعمل

═══════════════════════════════════════════════════════════
