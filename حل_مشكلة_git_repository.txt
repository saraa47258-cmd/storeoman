═══════════════════════════════════════════════════════════
   حل مشكلة: fatal: not a git repository
═══════════════════════════════════════════════════════════

❌ الخطأ: أنت في المجلد الخاطئ!

═══════════════════════════════════════════════════════════
✅ الحل:
═══════════════════════════════════════════════════════════

1️⃣  الانتقال إلى مجلد المشروع:
   cd /var/www/alshabibi/storeoman

2️⃣  التحقق من وجود .git:
   ls -la | grep .git

3️⃣  إذا لم يكن موجوداً، قم بتهيئة Git:
   git init
   git remote add origin https://github.com/saraa47258-cmd/storeoman.git

4️⃣  سحب التحديثات:
   git pull origin main

═══════════════════════════════════════════════════════════
📋 الأوامر الكاملة:
═══════════════════════════════════════════════════════════

cd /var/www/alshabibi/storeoman
git init
git remote add origin https://github.com/saraa47258-cmd/storeoman.git
git pull origin main
docker-compose restart

═══════════════════════════════════════════════════════════
